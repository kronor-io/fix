{-# LANGUAGE DeriveGeneric #-}
{-# LANGUAGE DerivingStrategies #-}
{-# LANGUAGE MultiParamTypeClasses #-}
{-# LANGUAGE RecordWildCards #-}
{-# OPTIONS_GHC -Wno-unused-imports #-}

-- | This module has been GENERATED by fix-codegen.
-- Any manual edits will be undone the next time fix-codegen is run.
module FIX.Components.CommissionData where

import Data.List.NonEmpty (NonEmpty)
import Data.Proxy
import Data.Validity
import FIX.Components.Class
import FIX.Fields.CommCurrency
import FIX.Fields.CommType
import FIX.Fields.Commission
import FIX.Fields.FundRenewWaiv
import FIX.Fields.MsgType
import FIX.Groups.Class
import GHC.Generics (Generic)

-- | ComponentSpec
--   { componentName = "CommissionData"
--   , componentPieces =
--       [ MessagePieceField "Commission" False
--       , MessagePieceField "CommType" False
--       , MessagePieceField "CommCurrency" False
--       , MessagePieceField "FundRenewWaiv" False
--       ]
--   }
data CommissionData = CommissionData
  { commissionDataCommission :: !(Maybe Commission),
    commissionDataCommType :: !(Maybe CommType),
    commissionDataCommCurrency :: !(Maybe CommCurrency),
    commissionDataFundRenewWaiv :: !(Maybe FundRenewWaiv)
  }
  deriving stock (Show, Eq, Generic)

instance Validity CommissionData

instance IsComponent CommissionData where
  toComponentFields ((CommissionData {..})) =
    mconcat
      [ optionalFieldB commissionDataCommission,
        optionalFieldB commissionDataCommType,
        optionalFieldB commissionDataCommCurrency,
        optionalFieldB commissionDataFundRenewWaiv
      ]
  fromComponentFields = do
    commissionDataCommission <- optionalFieldP
    commissionDataCommType <- optionalFieldP
    commissionDataCommCurrency <- optionalFieldP
    commissionDataFundRenewWaiv <- optionalFieldP
    pure (CommissionData {..})

makeCommissionData :: CommissionData
makeCommissionData =
  let commissionDataCommission = Nothing
      commissionDataCommType = Nothing
      commissionDataCommCurrency = Nothing
      commissionDataFundRenewWaiv = Nothing
   in (CommissionData {..})
